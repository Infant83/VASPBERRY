# v1.1.1 12 x 12 diagnostic snapshot

Status: **non-reportable diagnostic**. The guarded result is `valid=false`,
`z2=null`, and `candidate_z2=1`. Do not cite 1 as the Bi Z2 invariant from
this run.

This snapshot was generated by
[GitHub Actions run 33850537368](https://github.com/Infant83/VASPBERRY/actions/runs/33850537368)
from commit
[`fa4d14b`](https://github.com/Infant83/VASPBERRY/commit/fa4d14b71cfccf816376488d33bc528f14126aa7).
The workflow conclusion is `failure` because `run_z2.sh` returns nonzero when
any publication guard fails; both programs completed and the diagnostic
artifact was uploaded.
The input was the tracked 12 x 12 x 1 spinor `WAVECAR` with SHA-256
`a8d81854f2efc561938e478dde1be29a17ccc95d5d37325c122b9d9e82fa0838`.
Bands 1--10 form the occupied subspace; band 11 is included in the energy-gap
and time-reversal checks as the first unoccupied sentinel.

## Corrected Fortran reconstruction

`Z2_FIELD.csv` records `result_status=PASS` only for the numerical
self-consistency of the time-reversal reconstruction. It deliberately records
`reportable_invariant=0`.

| Check | Result |
|---|---:|
| top legacy candidate | 1 |
| bottom legacy candidate | 1 |
| top/bottom half-zone sums | -3 / +3 |
| total Chern residual | 1.15938e-14 |
| maximum TR-odd flux residual | 1.81521e-14 rad |
| maximum paired n-field residual | 0 |
| minimum raw link singular value | 0.815455 |

The top and bottom candidates now agree modulo 2, unlike the archived
pre-v1.1.1 result. Raw pointwise n-field values remain gauge- and logarithm-
branch-dependent; pointwise mirror equality is not an acceptance condition.
The Fortran link singular values are computed from unnormalized raw
coefficient frames and are therefore scale-dependent. They did not gate this
run, but they are not a normalized conditioning measure.

## Guarded Wilson-loop result

Both loop directions produce candidate 1 and agree with each other. Several
independent checks also pass: the direct and indirect gaps are 0.5924485 eV
and 0.5100444 eV; the occupied Chern residual is 2.37e-16; the maximum flux
TR residual is 1.57e-6 rad; and the minimum normalized link singular value is
0.815609.

Five guards still fail:

| Guard | Value | Required |
|---|---:|---:|
| x-loop WCC GAP ratio | 0.146990408 | > 0.3 |
| x-loop WCC MOVE ratio | 1.451000110 | < 0.3 |
| y-loop WCC GAP ratio | 0.146990371 | > 0.3 |
| y-loop WCC MOVE ratio | 1.450999785 | < 0.3 |
| occupied-projector TR residual | 1.89199e-5 | <= 1e-6 |

All four WCC failures occur between pump lines q=0 and q=1/12. The MOVE and
GAP thresholds are retained: accepting a MOVE ratio near 1.45 would allow a
WCC displacement larger than the relevant largest gap in one pump step. The
projector residual is small and may reflect the normal-precision VASP 5.4.1
source calculation, but it still exceeds the declared guard and is not waived.

The complex64 raw-norm comparison that failed in the preceding run is fixed
in `fa4d14b` by accumulating products and sums in float64. It now passes at
0.9999999999999941 against a minimum of 0.999999.

## Files

- `NFIELD.dat` and `Z2_FIELD.csv`: corrected Fortran candidate and
  self-consistency diagnostics.
- `z2_diagnostics.json`: authoritative guarded status, thresholds, values, and
  failed-guard list.
- `z2_wilson_wcc.csv` and `z2_wilson_wcc.png`: Wilson-loop spectra for both
  directions.
- `fortran.log` and `python.log`: captured program output from the Actions run.

The next validation step is a controlled full 24 x 24 x 1 Gamma-centered VASP
calculation with tighter electronic precision. If the first pump interval
remains under-resolved, use 48 x 48 or adaptive sampling near Gamma. A Z2 value
may be reported only after `valid=true`, `z2` is 0 or 1, both loop directions
agree, and every guard passes on a converged mesh.
